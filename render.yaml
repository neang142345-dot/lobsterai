services:
  - type: web
    name: lobsterai-web
    env: python
    plan: free
    buildCommand: "pip install flask"
    startCommand: "python app.py"
    envVars:
      - key: PORT
        value: 10000
